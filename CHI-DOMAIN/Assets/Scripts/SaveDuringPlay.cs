using UnityEngine;


/*

namespace Unity.Cinemachine {
    public sealed class SaveDuringPlayAttribute : System.Attribute {}
    public sealed class NoSaveDuringPlayAttribute : PropertyAttribute {}
}

using Unity.Cinemachine;

[SaveDuringPlay]
public class SaveDuringPlay : MonoBehaviour
{
    public float speed = 10f;
    [NoSaveDuringPlay] public float maxSpeed = 20f;
    public Vector3 trackedOffset;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}



*/
